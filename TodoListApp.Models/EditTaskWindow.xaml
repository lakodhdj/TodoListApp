<Window x:Class="TodoListApp.EditTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактировать задачу" Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Название:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="1" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
             MinHeight="25" Margin="0,0,0,10"/>

        <Label Grid.Row="2" Content="Описание:" Margin="0,0,0,5"/>
        <TextBox Grid.Row="3" Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
             TextWrapping="Wrap" AcceptsReturn="True" 
             VerticalScrollBarVisibility="Auto"
             MinHeight="100" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="Отмена" Click="CancelButton_Click" Margin="5" Padding="10,5"/>
            <Button Content="Сохранить" Click="SaveButton_Click" Margin="5" Padding="10,5"/>
        </StackPanel>

    </Grid>
</Window>